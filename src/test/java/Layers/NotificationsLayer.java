package Layers;

import Pages.BasePage;
import org.openqa.selenium.WebDriver;

public class NotificationsLayer extends BasePage implements INotificationsLayer {
    public NotificationsLayer(WebDriver driver) {
        super(driver);
    }
}
